version: "3.8"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MODEL_PATH=/app/outputs/best_model.pth
    depends_on:
      - redis
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    restart: unless-stopped
